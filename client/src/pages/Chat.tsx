import { Navigate } from 'react-router-dom';

// This is just a placeholder file to prevent Tailwind CSS scanning errors
// The actual implementation is in ChatEnhanced.tsx
export default function Chat() {
  return <Navigate to="/chat-enhanced" replace />;
} 